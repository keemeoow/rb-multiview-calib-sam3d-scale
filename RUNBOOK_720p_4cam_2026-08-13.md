# 4대 @720p 전체 재캘리브 → 물체 촬영 → 크기 추정 런북

작성일: 2026-08-13
대상: static 4대 (D415 ×3 + D435 ×1), 해상도 1280×720
이전 런북: `RUNBOOK_720p_recalib_2026-07-16.md` (static 3대 버전)

---

## 0) 사전 조건 (여기서 막히면 뒤가 전부 깨진다)

### 0-1. USB — 4대 모두 USB3여야 함
```bash
/home/sprout/anaconda3/envs/multicam_cube/bin/python3 -c "
import pyrealsense2 as rs
for d in rs.context().query_devices():
    print(d.get_info(rs.camera_info.serial_number),
          d.get_info(rs.camera_info.name),
          'USB'+d.get_info(rs.camera_info.usb_type_descriptor))"
```
**4대 전부 `USB3.2`가 떠야 한다.** `USB2.1`이 하나라도 있으면 중단.

- USB2.1 모드에서는 **depth 1280×720이 6fps만** 지원된다 → `--depth_fps 15`는 프로파일 해석 실패로 그 카메라가 통째로 스킵된다.
- 대역폭도 못 버틴다. 720p color+depth 1대 @6fps ≈ 28 MB/s인데 USB2 버스는 실효 ≈40 MB/s. 2대만 물려도 초과.
- 원인은 대개 **USB2용 micro-B 케이블**. 허브가 USB3여도 케이블이 USB2면 USB2로 협상된다.
- 조치: 카메라 케이블을 USB3로 교체 + 허브 데이지체인(허브→허브) 피하고 본체 포트 직결.

토폴로지 확인: `lsusb -t` — RealSense가 붙은 줄이 `5000M`이어야 한다 (`480M`이면 USB2).

### 0-2. 환경
| 단계 | 환경 |
|---|---|
| 0~4 (캘리브·촬영) | `multicam_cube` — `pyrealsense2` 있음 |
| 5 (SAM2 마스킹) | `sam2env` |
| 6 (SAM3D 크기추정) | SAM3D 전용 환경 (`sam3d_env_gb10.sh` 참고) |

`sam2env`에는 `pyrealsense2`가 없으므로 0~4단계를 여기서 돌리면 `ModuleNotFoundError`가 난다.

```bash
CAM_PY=/home/sprout/anaconda3/envs/multicam_cube/bin/python3
```

### 0-3. fps는 6으로
캘리브도 물체 촬영도 **정지 장면**이라 fps가 낮아도 무방하다.
4대가 한 USB3 컨트롤러를 공유하면 720p×4 @15fps는 ≈276 MB/s로 빠듯하다.
**K는 해상도에만 의존하고 fps와 무관**하므로 6fps로 떠도 인트린식 품질은 동일하다.

### 0-4. D435를 4번째 static으로 쓸 때
topview 등으로 **고정**해 놓고 큐브를 손으로 옮긴다.
EE가 큐브를 잡으면 팔에 달린 그리퍼 카메라도 같이 움직여서 static 가정이 깨진다.

---

## 1) intrinsics 덤프 @720p (4대)

```bash
$CAM_PY Calib_Step1_dump_intrinsics.py \
  --color_w 1280 --color_h 720 --color_fps 6 \
  --depth_w 1280 --depth_h 720 --depth_fps 6
```

**`--fresh_map` 절대 쓰지 말 것.** 기존 `device_map.json`을 유지하면
314→cam0 / 319→cam1 / 912→cam2가 그대로 보존되고 D435(752)만 **cam3**으로 붙는다.
`--fresh_map`을 쓰면 시리얼 정렬(314, 319, 752, 912)로 D435가 cam2를 차지해 인덱스가 전부 밀린다.

**확인:**
- `[SAVE] intrinsics/cam0.npz ~ cam3.npz` 4개 전부
- 맨 끝에 `[WARN] N camera(s) failed to dump` 가 **없어야** 함
- `intrinsics/device_map.json`의 `serial_to_idx`에 4개 시리얼

```bash
$CAM_PY -c "
import numpy as np,glob
for f in sorted(glob.glob('intrinsics/cam*.npz')):
    d=np.load(f,allow_pickle=True)
    print(f, d['serial'], f\"{d['color_w']}x{d['color_h']}\", 'fx=%.1f cx=%.1f'%(d['color_K'][0,0],d['color_K'][0,2]))"
```
4줄 모두 `1280x720`이어야 한다.

---

## 2) 큐브 멀티캠 촬영 @720p

```bash
$CAM_PY Calib_Step2_capture_multi_cam.py \
  --root_folder ./data/static_cams_session_03 \
  --intrinsics_dir ./intrinsics \
  --width 1280 --height 720 --fps 6 \
  --min_markers 2 --show --preview_scale 0.4
```

- 프리뷰는 2×2 그리드(4패널). 720p 4패널 = 2560×1440이라 `--preview_scale 0.4` → 1024×576으로 표시.
  저장 이미지는 원본 해상도 그대로다.
- 큐브를 다양한 위치·자세로 옮기며 **4대가 동시에 잘 보는 프레임**을 수십 장 저장.
- 특정 카메라만 프레임 수가 적으면 그 카메라의 외부 파라미터 정확도가 떨어진다 — 4대 모두 고르게.
- 팔/그리퍼가 큐브를 가리지 않게 한다.

---

## 3) 캘리브레이션 (4대 한번에)

```bash
$CAM_PY Calib_Step3_calibrate_multi_cam_cube.py \
  --root_folder ./data/static_cams_session_03 \
  --intrinsics_dir ./intrinsics \
  --ref_cam_idx 0 --min_markers 1 --save_overlay
```

**확인:**
- 끝에 `reproj_rms_px` — 1px 내외면 양호 (session_02는 0.573)
- 결과: `./data/static_cams_session_03/calib_out_cube/transforms/T_C0_Ci_all.json`
- 그 JSON의 `T_Cref_Ci`에 키 `"0","1","2","3"` **4개가 다 있어야** 한다. 빠진 카메라가 있으면
  그 카메라는 큐브 공통 관측이 부족한 것 → 2단계로 돌아가 그 카메라 위주로 더 찍는다.

```bash
$CAM_PY -c "
import json;d=json.load(open('data/static_cams_session_03/calib_out_cube/transforms/T_C0_Ci_all.json'))
print('rms_px', d['reproj_rms_px']); print('cams', sorted(d['T_Cref_Ci'].keys()))"
```

---

## 4) 물체 촬영 @720p (K·extrinsic 자동 생성)

```bash
$CAM_PY Obj_Step1_capture_object.py \
  --out_dir ./data/capture_obj_04 \
  --intrinsics_dir ./intrinsics \
  --transforms_json ./data/static_cams_session_03/calib_out_cube/transforms/T_C0_Ci_all.json \
  --width 1280 --height 720 --fps 6 \
  --depth_burst_n 10 --show --preview_scale 0.4
```
SPACE = 저장 후 종료, q/ESC = 취소.

`--width/--height` 기본값이 640×480이므로 **반드시 명시**해야 한다.
`--transforms_json`을 넣었으므로 `cam0~3_K.txt`, `cam0~3_T_cam_to_world.txt`, `calib_info.json`이 자동 생성된다.

**확인:** `data/capture_obj_04/`에 카메라당 4개씩 총 16개 + `calib_info.json`

---

## 5) 마스킹 (SAM2, sam2env)

```bash
SET="data/capture_obj_04"; MASKS="data/masks_04"
PYTHONWARNINGS=ignore /home/sprout/anaconda3/envs/sam2env/bin/python3 \
  Obj_Step2_mask_sam2.py --capture_dir "$SET" --masks_dir "$MASKS" \
  --sam_checkpoint ~/sam2_checkpoints/sam2_hiera_large.pt \
  --sam_config configs/sam2/sam2_hiera_l.yaml --num_objects 1 --device cpu
```
물체가 2개면 `--num_objects 2`.

---

## 6) 크기 추정 (SAM3D 환경)

```bash
python Obj_Step3_sam3d_scale.py \
  --data_dir ./data/capture_obj_04 --mask_dir ./data/masks_04 --out_dir ./data/outputs_04 \
  --depth_scale 0.001 --mask_erode_px 3 --keep_largest_cc \
  --run_sam3d --sam3d_cam cam1 --spconv_algo native \
  --estimate_size --size_save_overlay
```

출력:
- `<obj>_sam3d_scaled.glb` — 실척·meter·원점중심 (FoundationPose/Isaac 입력)
- `<obj>_size.json` — scale, 치수, per-view IoU
- `<obj>_size_overlay.jpg`

**IoU가 `--size_min_iou`(기본 0.85) 미만이면** SAM3D 형상이 관측과 어긋난 것이라 치수를 믿으면 안 된다.
`--sam3d_cam`을 물체가 더 잘 보이는 카메라로 바꿔 재시도한다. 이제 cam3까지 4개 후보가 있다.

---

## 참고

- **엑스트린식은 해상도와 무관**하지만 **인트린식은 해상도 종속**이다.
  640×480(4:3)과 1280×720(16:9)은 단순 스케일 관계가 아니라 FOV 크롭이 다르므로 K를 환산해 쓸 수 없다.
- 카메라를 물리적으로 건드리면 엑스트린식이 무효화된다 → 2~3단계 재실행.
  인트린식(1단계)은 카메라를 옮겨도 유효하다.
- 크기 추정에는 robot base 좌표계가 필요 없다 (cam0 기준으로 충분).
  base 정합이 필요하면 EE에 큐브를 장착한 eye-to-hand 촬영 + `Calib_Step3ee`/`Calib_Step4ee`가 별도로 필요하다.
